interface:
  display_name: "Build Frontends"
  short_description: "Build, fix, polish, and verify browser UI"
  default_prompt: "Use $build-frontends for this browser frontend task. Follow the existing stack and verify the requested result proportionately."
policy:
  allow_implicit_invocation: true
