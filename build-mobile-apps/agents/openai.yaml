interface:
  display_name: "Build Native Android Apps"
  short_description: "Build native Android screens and app flows"
  default_prompt: "Use $build-mobile-apps to implement this native Android app flow in the project's existing stack."
policy:
  allow_implicit_invocation: true
