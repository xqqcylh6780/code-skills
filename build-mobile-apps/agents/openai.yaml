interface:
  display_name: "Build Mobile Apps"
  short_description: "Build uni-app, mobile, and mini-program flows"
  default_prompt: "Use $build-mobile-apps to implement this mobile or cross-platform app flow for the actual target runtime."
policy:
  allow_implicit_invocation: true
