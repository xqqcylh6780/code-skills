interface:
  display_name: "Review Changes"
  short_description: "Review code changes for correctness and risk"
  default_prompt: "Use $review-changes to review these code changes against their requirements and report evidence-backed findings."
